# Stage 07 — Settings UI

**Gate**: Yes

## Objective

Add provider configuration UI in the taskpane.

## Scope

- `src/ui/settings/` — provider settings component.
- API key fields with redaction.
- Persist settings via `core/state/persistence.ts`.

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `docs/project-state.md` updated

## Status

PENDING
